import React from 'react';

export default function Health() {
  return (
    <div style={{padding:40,fontFamily:'system-ui, sans-serif'}}>
      <h1>OK - served from next-app/pages/__health</h1>
      <p>This confirms the Next app (next-app) is being built and served.</p>
    </div>
  );
}
